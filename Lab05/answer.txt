- The Aims class must be updated to handle any exceptions generated when the play() methods are
called. What happens when you don’t update for them to catch?
Trả lời:
Nếu bạn không try-catch, khi gặp một media có độ dài không hợp lệ (ví dụ: length = 0), chương trình sẽ bị "crash" (dừng hoạt động ngay lập tức) và hiển thị một đống lỗi trong console, gây khó hiểu cho người dùng.